const express = require('express');
const router = express.Router();
// const {} = require('../controllers/uploadonreoller');

// // Route to update admin settings
// router.post('/upload', );
// //get all settings
// router.get('/manage/', );

module.exports = router;
